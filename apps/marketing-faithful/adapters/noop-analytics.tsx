'use client'

// Marketing adapter: visual composition remains unchanged; event capture is intentionally absent until a public analytics plan is chosen.
export function LandingAnalytics() {
  return null
}
