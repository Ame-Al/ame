// Faithful marketing route: reuse the original landing composition, with shared shell provided by app/layout.tsx.
import Landing from '../../sim/app/(landing)/landing'

export default function MarketingHomePage() {
  return <Landing />
}
