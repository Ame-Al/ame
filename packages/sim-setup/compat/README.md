# sim-setup compatibility alias

`sim-setup` preserves existing automation after the CLI was renamed to [`ame-setup`](https://www.npmjs.com/package/ame-setup).

For new installations, use:

```bash
npx ame-setup
```
